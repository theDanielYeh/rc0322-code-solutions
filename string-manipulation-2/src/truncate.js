/* exported truncate */
function truncate(length, string) {
  return string.substr(0, length) + '...';

  // var newString = '';
  // newString = newString.concat(string.slice(0, length), '...');

  // return newString;
}
// for loop that extracts and adds to new string? concat?
